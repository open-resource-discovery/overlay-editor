// Ambient declarations for CSS-only side-effect imports used in the website.
// TypeScript needs a module declaration for the `./styles` subpath that ships
// as raw CSS with no accompanying `.d.ts`.
declare module '@open-resource-discovery/ui-components/styles';
